
<div class="prepend-1", id="show-rsvp">
<h1>Thanks for sending us your RSVP!</h1>
<p>Please look over your information. If you need to correct anything
you may <%= link_to 'edit', edit_rsvp_path(@rsvp) %> it now.<br />
If something changes later, you can return to the site and login
with your email and password to edit your RSVP.</p>
<p>
  <h3>User:</h3>
  <div class="prepend-1">
  <b>Name:</b>
  <%= @rsvp.user.name %>
</p>
<p>
  <b>Email:</b>
  <%= @rsvp.user.email %>
</p>
  </div>
<p>
  <h3>Guests:</h3> 
<table id="table-5" class="table">
<thead><th>Name</th><th>Wedding</th><th>Reception</th></thead>
<tbody id="argh">
   <% for guest in @rsvp.guests %>
     <tr><td><%=h guest.name %></td>
         <td><%= guest.wedding.capitalize %></td>
         <td><%= guest.reception.capitalize %></td>
     </tr>
   <% end %>
</tbody>
</table>
<% if !@rsvp.user.address_line_1.empty? %>
  <p>
    <h3>Address:</h3>
    <div class="prepend-1">
    <%= @rsvp.user.address_line_1 %>
    <% if !@rsvp.user.address_line_2.empty? %>
      <%= @rsvp.user.address_line_2 %>
    <% end %>
    <%= @rsvp.user.city %>, <%= @rsvp.user.state %>  <%= @rsvp.user.zip %>
    </div>
  </p>
<% end %>
<p>
  <h3>Comment:</h3>
  <div class="prepend-1">
  <%= @rsvp.comment %>
  </div>
</p>

<%= link_to 'Edit RSVP', edit_rsvp_path(@rsvp) %>
</div>
